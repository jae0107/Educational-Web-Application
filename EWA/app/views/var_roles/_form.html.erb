<%= form_with model: @var_role, local: true do |form| %>

	<% if @var_role.errors.any? %>
		<div id="error_explanation">
			<h2>
				<%= pluralize(@var_role.errors.count, "error") %> prohibited this article from being saved:
			</h2>
			<ul>
				<% @var_role.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<p>
		<%= form.label :VarRole, 'Variable Role' %><br>
		<%= form.text_field :VarRole %>
	</p>

	<p>
		<%= form.label :VarRoleDescription, 'Description' %><br>
		<%= form.rich_text_area :VarRoleDescription, :cols=>80, :rows=>10 %>
	</p>

	<p>
		<%= form.label :example, 'Example' %><br>
		<%= form.rich_text_area :example, :cols=>80, :rows=>10 %>
	</p>

	<p>
		<%= form.submit 'Save'%>
	</p>

<% end %>
